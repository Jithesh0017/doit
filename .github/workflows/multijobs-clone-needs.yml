name: multi-job
on: workflow_dispatch
jobs:
  first_job:
    needs: second_job
    runs-on: ubuntu-latest
    steps:
      - name: maa
        run: echo "super"
      - name: cloning
        uses: actions/checkout@v4.2.2
      - name: files
        run: ls -a

  second_job:
    runs-on: ubuntu-latest
    steps:
      - name: dad
        run: echo "excellent"
